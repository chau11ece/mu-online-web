services:

  mu-web:
    build: ./Web
    container_name: mu-web
    env_file: .env
    environment:
      - PUBLIC_IP=${PUBLIC_IP}
      - SQL_SERVER=192.168.100.96,1433
    ports:
      - "8080:80"
      