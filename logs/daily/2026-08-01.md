# Daily Log — 2026-08-01 — Mu Online Web

## Today's Assignment
No day-XX training assignment — this is real project work. Planned: scope out hosting/scaling strategy for the danangmu.com launch and start executing the highest-priority infra fix.

## Completed
- Sized hosting platforms and instance/cost tiers for Stage 1 (≤1,000 CCU) → Stage 3 (20-50K CCU), with an honest reality check on the stated 1M-concurrent target (not achievable on this game engine; sized to realistic tiers instead)
- Agreed a phased infra build order: Phase 0 (stabilize existing state) → Phase 1 (DB) → Phase 2 (anti-DDoS/proxy) → Phase 3 (game server farm) → Phase 4 (website hardening/redeploy), cost-savings as the standing priority
- **Found and fixed a live credential exposure**: `ansible-mu/inventory/group_vars/{dbservers,webservers}.yml` had the production MSSQL `sa` password hardcoded in plaintext, pushed to `github.com/chau11ece/ansible-mu`. The repo also had no `.gitignore`, so the Ansible Vault decryption key (`vault/.vault_pass`) was committed too, defeating vault entirely.
  - Rotated the password, moved it into `vault/secrets.yml` (properly vault-encrypted this time) as `vault_mssql_sa_password`, added `.gitignore`, untracked `vault/.vault_pass`
  - Committed + pushed: `ansible-mu` commit `e3e47f1`
- Confirmed via CI workflow inspection that `ANSIBLE_VAULT_PASSWORD` and `SSH_PRIVATE_KEY` are GitHub Actions secrets on **this** repo (`mu-online-web`), not `ansible-mu` — needed since I don't have local SSH access to the droplets (the real deploy key only exists as a CI secret)
- Verified actual production state (read-only, no SSH key available locally): **all three droplets are effectively down** — web app not listening on its port (bare nginx 404), MSSQL not listening (connection refused), game server droplet fully unreachable (SSH/80/443 all timeout)

## Not Completed
- GitHub secret `ANSIBLE_VAULT_PASSWORD` on this repo still needs manual update to match the rotated vault password (I can't set GitHub secrets myself) — blocks the next CI deploy
- Game server droplet unreachability and host-key mismatches on the web/DB droplets not yet investigated (likely explained by a past droplet rebuild, unconfirmed)
- Phase 1-4 of the infra plan not started — Phase 0 stabilization surfaced a bigger issue than expected and consumed the session

## Issues Hit and How We Solved Them
- **Stale assumption corrected mid-session**: at session start, `git status` showed 17 modified-but-uncommitted files in this repo matching a "security refactoring" I'd logged in memory as happening 2026-04-01. Investigation during `/report` showed this was already committed as `540d726` on **2026-07-28** (memory had the wrong date) — and the working-tree diff on those 14 files had resolved itself by the time I checked again (not something I did; likely the user's own parallel terminal activity). Corrected the memory date.
- **New finding surfaced by the above check**: one file in that original diff, `Web/constants.php`, still has a real uncommitted change sitting in the working tree — and it's dangerous, not benign. It sets `MD5` from `2` to `0` (would store passwords in **plaintext**), hardcodes `HOST` to `mu-db-dev:1433` (a dev-only hostname, would break prod DB connectivity if deployed), and hardcodes the same leaked `Abcd@1234` password found in `ansible-mu`. This looks like local dev-environment scratch state, not something ready to commit. Flagged for the user, did not touch it.

## Extra Things Explored
- `deploy-production-errors.txt` confirmed the last Ansible run was cancelled mid `docker-compose up` — consistent with today's finding that prod is actually down
- `.claude/agents/kfc/`, `.claude/settings/kfc-settings.json`, `.claude/system-prompts/` are still sitting untracked on disk even though the "What Has Been Built" table already marked kfc cleanup ✅ (commits `571610d`/`e32ff7f` only stopped tracking them in git, never deleted them from disk)

## Artifacts Built Today
- `ansible-mu` commit `e3e47f1` — credential rotation + vault fix (pushed)
- Hub memory: `infra_scaling_plan.md` (new) — phased infra plan, cost-tier reference table
- Hub memory: `MEMORY.md` index — added Infrastructure section pointer, corrected Security Refactoring date from 2026-04-01 to 2026-07-28
- This repo's `CLAUDE.md` — "What Has Been Built" table updated with today's items and two new open items

## How I Used Claude Code Today
Used it to reason through hosting/cost tradeoffs for a real infra decision, then to actually investigate live production state (git archaeology across two repos, TCP-level connectivity checks against real droplets, CI workflow inspection to find where secrets actually live) rather than just generating config from assumptions.

## Blockers / Questions for Mentor
Is there an existing convention for where machine secrets like the new Ansible Vault password should live long-term (password manager vs. the Infisical instance already pointed to at secrets.danangmu.com), or is "local gitignored file only" acceptable for a single-operator early stage?

## Self Score
- Completion: 6/10 — Phase 0 stabilization is bigger than scoped; credential fix landed but deploy itself hasn't run yet
- Understanding: 8/10 — clear picture of why the game/web/DB tiers scale differently now, and exactly where the current infra is broken
- Energy: 7/10

## One Thing I Learned Today That Surprised Me
The already-committed "security refactor" I thought was still sitting uncommitted from months ago had actually landed days ago — and the file I assumed was part of that same safe cleanup (`constants.php`) was actually a separate, currently-uncommitted change that would have shipped plaintext password storage to production if it had been swept in accidentally.

## Tomorrow's Context Block
Production is confirmed down on all three droplets and the credential fix is pushed to `ansible-mu` but not yet live. First thing next session: decide on `Web/constants.php` (revert vs. extract into a gitignored local-only override), update the `ANSIBLE_VAULT_PASSWORD` GitHub secret, then trigger a real CI/CD deploy to bring production back up on the corrected credentials before starting Phase 1 (the DB buildout).
