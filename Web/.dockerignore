# Exclude binary/archive files to reduce build context size
**/*.exe
**/*.bak
**/*.zip
**/*.rar

# PHP executables are required — re-include the entire php/ directory
!php/
!php/**

# General exclusions
.git/
*.log
*.tmp
